{
    "extends": "../../tsconfig.base.json",
    "compilerOptions": {
        "outDir": "./dist/client",
        "types": ["@citizenfx/client"],
    },

    "include": ["./**/*.ts", "../shared/**/*.ts"]
}
