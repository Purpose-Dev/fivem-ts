{
    "extends": "../../tsconfig.base.json",
    "compilerOptions": {
        "outDir": "./dist/server",
        "types": ["@citizenfx/server", "@types/node"]
    },

    "include": ["./**/*.ts", "../shared/**/*.ts"]
}
