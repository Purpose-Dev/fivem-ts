{
    "extends": "../../tsconfig.base.json",
    "compilerOptions": {
        "outDir": "./dist/server",
        "types": ["@citizenfx/server"],
    },

    "include": ["./**/*.ts", "../shared/**/*.ts"]
}
