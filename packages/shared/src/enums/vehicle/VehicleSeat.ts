/**
 * Enum representing different types of vehicle seats.
 *
 * @enum{number}
 */
export enum VehicleSeat {
    /** Indicates the absence of a seat. */
    None = -3,
    /** Can represent any seat in the vehicle. */
    Any = -2,
    /** Represents the driver's seat. */
    Driver = -1,
    /** Represents a generic passenger seat. */
    Passenger = 0,
    /** Represents the left front seat (typically the same as the driver's seat). */
    LeftFront = -1,
    /** Represents the right front seat (typically the front passenger seat). */
    RightFront = 0,
    /** Represents the left rear seat. */
    LeftRear = 1,
    /** Represents the right rear seat. */
    RightRear = 2,
    /** Represents additional seat, useful for vehicles with more than the standard number of seats. */
    ExtraSeat1 = 3,
    /** Represents additional seat, useful for vehicles with more than the standard number of seats. */
    ExtraSeat2 = 4,
    /** Represents additional seat, useful for vehicles with more than the standard number of seats. */
    ExtraSeat3 = 5,
    /** Represents additional seat, useful for vehicles with more than the standard number of seats. */
    ExtraSeat4 = 6,
    /** Represents additional seat, useful for vehicles with more than the standard number of seats. */
    ExtraSeat5 = 7,
    /** Represents additional seat, useful for vehicles with more than the standard number of seats. */
    ExtraSeat6 = 8,
    /** Represents additional seat, useful for vehicles with more than the standard number of seats. */
    ExtraSeat7 = 9,
    /** Represents additional seat, useful for vehicles with more than the standard number of seats. */
    ExtraSeat8 = 10,
    /** Represents additional seat, useful for vehicles with more than the standard number of seats. */
    ExtraSeat9 = 11,
    /** Represents additional seat, useful for vehicles with more than the standard number of seats. */
    ExtraSeat10 = 12,
    /** Represents additional seat, useful for vehicles with more than the standard number of seats. */
    ExtraSeat11 = 13,
    /** Represents additional seat, useful for vehicles with more than the standard number of seats. */
    ExtraSeat12 = 14,
}
