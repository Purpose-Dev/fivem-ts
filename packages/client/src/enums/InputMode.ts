/**
 * Enum representing the input modes available for user interaction.
 *
 * The `InputMode` enum defines the mode of input being used by the user,
 * such as a combination of mouse and keyboard or a gamepad.
 *
 * @enum{number}
 */
export enum InputMode {
    MouseAndKeyboard = 0,
    GamePad = 2,
}
